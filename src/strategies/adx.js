export const id = 'adx';
export const defaultParams = { period: 14 };

export function generateSignals(_candles, _params = {}) {
  return [];
}

export default { id, defaultParams, generateSignals };
